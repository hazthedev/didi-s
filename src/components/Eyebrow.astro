---
export interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<span class={`eyebrow text-sienna font-medium ${className}`}>
  {text}
</span>
